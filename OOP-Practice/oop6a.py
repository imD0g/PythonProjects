# # Python program to demonstrate in-built polymorphic functions

# # len() function being used on a string
# print("len() function being used on a string:", len("Cameron-noremaC"))

# #len() function being used on a list
# print("len() function being used on a list:", len([1, 2, 3, 4, 5]))


# user defined polymorphic function that adds x, y and z
def add(x, y, z):
      return x + y + z

# Code for using the function
print("Using the addition function:", add(1, 2, 3))
